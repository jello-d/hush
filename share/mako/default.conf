# hush's default notification placement -- a single panel, top-right on the
# focused output. mako-placement falls back to this when there is no shared
# ~/.config/shapes/<shape>/mako.conf (a standalone box, or a single-panel one).
# It writes it verbatim to mako/placement.active (the include mako reads).
anchor=top-right
